import { Box, Button, Typography } from "@mui/material";

const Navbar = () => {
  return (
    <Box>
      <Typography mt="20px" variant="h1" sx={{fontSize:"30px"}}>Countries Around the World</Typography>
    </Box>
  );
};

export default Navbar;
